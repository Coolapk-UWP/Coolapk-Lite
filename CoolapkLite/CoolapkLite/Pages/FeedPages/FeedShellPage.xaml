<Page
    x:Class="CoolapkLite.Pages.FeedPages.FeedShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CoolapkLite.Pages.FeedPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:control="using:CoolapkLite.Controls" xmlns:feedpages="using:CoolapkLite.ViewModels.FeedPages" d:DataContext="{d:DesignInstance Type=feedpages:FeedShellViewModel}"
    mc:Ignorable="d">
    <Grid>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition />
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <control:TwoPaneView
            x:Name="TwoPaneView"
            Loaded="TwoPaneView_Loaded"
            MinWideModeWidth="804"
            Margin="{x:Bind StackPanelMargin}"
            ModeChanged="TwoPaneView_ModeChanged"
            Pane1Length="420"
            PanePriority="Pane2"
            TallModeConfiguration="SinglePane">
            <control:TwoPaneView.Pane1>
                <ScrollViewer x:Name="DetailScrollViewer" VerticalAlignment="Top">
                    <Grid x:Name="Pane1Grid">
                        <control:FeedShellDetailControl FeedDetail="{Binding FeedDetail}" x:Name="DetailControl" />
                    </Grid>
                </ScrollViewer>
            </control:TwoPaneView.Pane1>
            <control:TwoPaneView.Pane2>
                <Grid x:Name="RightGrid">
                    <control:FeedShellListControl x:Name="ListControl">
                        <Grid x:Name="Pane2Grid" />
                    </control:FeedShellListControl>
                </Grid>
            </control:TwoPaneView.Pane2>
        </control:TwoPaneView>
        <control:TitleBar
            x:Name="TitleBar"
            Title="{Binding Title}"
            BackButtonVisibility="Collapsed"
            BackgroundVisibility="Visible"
            RefreshButtonVisibility="Visible" />
    </Grid>
</Page>
